<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genio Visión - Tareas Dirigidas Carabobo</title>
    <script src="https://cdn.jsdelivr.net/npm/emailjs-com@3/dist/email.min.js"></script>
    <style>
        :root {
            --pastel-blue: #A8D5E2;
            --pastel-pink: #F4C2C2;
            --pastel-yellow: #FEE9B3;
            --pastel-green: #C4E4C4;
            --pastel-purple: #D7BDE2;
            --soft-white: #F8F9FA;
            --text-dark: #2C3E50;
            --shadow-light: 0 8px 32px rgba(0,0,0,0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--pastel-blue) 0%, var(--pastel-purple) 100%);
            color: var(--text-dark);
            line-height: 1.6;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Hero */
        .hero {
            text-align: center;
            padding: 80px 20px;
            background: rgba(255,255,255,0.9);
            border-radius: 20px;
            margin-bottom: 40px;
            box-shadow: var(--shadow-light);
            animation: fadeInUp 1s ease-out;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4rem);
            background: linear-gradient(45deg, var(--pastel-yellow), var(--pastel-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 20px;
            font-weight: 700;
        }

        .hero .subtitle {
            font-size: 1.5rem;
            color: var(--text-dark);
            margin-bottom: 30px;
            opacity: 0.9;
        }

        .cta-whatsapp {
            display: inline-block;
            background: linear-gradient(45deg, #25D366, #128C7E);
            color: white;
            padding: 18px 40px;
            border-radius: 50px;
            text-decoration: none;
            font-size: 1.3rem;
            font-weight: 600;
            box-shadow: 0 10px 30px rgba(37,211,102,0.4);
            animation: pulse 2s infinite;
            transition: all 0.3s ease;
        }

        .cta-whatsapp:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(37,211,102,0.6);
        }

        /* Services Grid */
        .services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin: 60px 0;
        }

        .service-card {
            background: rgba(255,255,255,0.95);
            border-radius: 20px;
            padding: 30px;
            text-align: center;
            box-shadow: var(--shadow-light);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--pastel-pink), var(--pastel-yellow), var(--pastel-green));
        }

        .service-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 20px 50px rgba(0,0,0,0.15);
        }

        .service-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            background: linear-gradient(135deg, var(--pastel-blue), var(--pastel-purple));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: white;
            animation: iconFloat 3s ease-in-out infinite;
        }

        .service-card:nth-child(1) .service-icon { animation-delay: 0s; }
        .service-card:nth-child(2) .service-icon { animation-delay: 0.5s; }
        .service-card:nth-child(3) .service-icon { animation-delay: 1s; }
        .service-card:nth-child(4) .service-icon { animation-delay: 1.5s; }

        /* Plans */
        .plans {
            background: rgba(255,255,255,0.9);
            border-radius: 20px;
            padding: 50px;
            margin: 60px 0;
            text-align: center;
        }

        .plan-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 30px 0;
            flex-wrap: wrap;
        }

        .plan-btn {
            padding: 15px 30px;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .plan-ausubel { background: var(--pastel-green); color: white; }
        .plan-piaget { background: var(--pastel-purple); color: white; }

        .plan-details {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--text-dark);
            margin-top: 20px;
        }

        .limited-spots {
            background: linear-gradient(45deg, var(--pastel-yellow), var(--pastel-pink));
            color: #D32F2F;
            padding: 15px;
            border-radius: 15px;
            font-weight: 700;
            margin-top: 20px;
            animation: bounce 2s infinite;
        }

        /* Testimonials */
        .testimonials {
            background: rgba(255,255,255,0.9);
            border-radius: 20px;
            padding: 50px;
            margin: 60px 0;
        }

        .testimonial {
            display: flex;
            align-items: center;
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeInRight 0.8s forwards;
        }

        .testimonial:nth-child(2) { animation-delay: 0.2s; }
        .testimonial:nth-child(3) { animation-delay: 0.4s; }
        .testimonial:nth-child(4) { animation-delay: 0.6s; }
        .testimonial:nth-child(5) { animation-delay: 0.8s; }

        .testimonial-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: var(--pastel-pink);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            margin-right: 20px;
            color: white;
        }

        .testimonial-text {
            flex: 1;
            font-style: italic;
            position: relative;
        }

        .testimonial-text::before {
            content: '"';
            font-size: 3rem;
            color: var(--pastel-yellow);
            position: absolute;
            left: -20px;
            top: -10px;
        }

        /* Contact Form */
        .contact-section {
            background: rgba(255,255,255,0.9);
            border-radius: 20px;
            padding: 50px;
            margin: 60px 0;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--text-dark);
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 15px;
            border: 2px solid var(--pastel-blue);
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: rgba(255,255,255,0.8);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--pastel-purple);
            box-shadow: 0 0 20px rgba(215,189,226,0.3);
        }

        .btn-submit {
            background: linear-gradient(45deg, var(--pastel-pink), var(--pastel-yellow));
            color: var(--text-dark);
            border: none;
            padding: 18px 50px;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(244,194,194,0.4);
        }

        /* Location */
        .location {
            text-align: center;
            margin: 60px 0;
        }

        .map-container {
            position: relative;
            height: 400px;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: var(--shadow-light);
            margin-top: 30px;
        }

        .map-container iframe {
            width: 100%;
            height: 100%;
            border: 0;
        }

        .footer {
            text-align: center;
            padding: 40px;
            background: rgba(255,255,255,0.7);
            border-radius: 20px;
            margin-top: 60px;
        }

        /* Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        @keyframes iconFloat {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        @keyframes fadeInRight {
            from { opacity: 0; transform: translateX(50px); }
            to { opacity: 1; transform: translateX(0); }
        }

        @media (max-width: 768px) {
            .hero { padding: 60px 20px; }
            .services { grid-template-columns: 1fr; }
            .plan-buttons { flex-direction: column; align-items: center; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Hero Section -->
        <section class="hero">
            <h1>🧠 Genio Visión</h1>
            <p class="subtitle">Tareas Dirigidas que Transforman Futuros</p>
            <p style="font-size: 1.3rem; margin-bottom: 30px; font-weight: 600;">
                Clases personalizadas • Apoyo escolar • Éxito garantizado
            </p>
            <a href="https://wa.me/584244517644?text=¡Hola!%20Quiero%20información%20sobre%20Genio%20Visión%20🚀" 
               class="cta-whatsapp" target="_blank">
                📲 ¡Reserva tu cupo YA! WhatsApp
            </a>
        </section>

        <!-- Services -->
        <section class="services">
            <div class="service-card">
                <div class="service-icon">💻</div>
                <h3>Computación Básica</h3>
                <p>Domina Word, Excel, PowerPoint y internet desde cero. ¡Clases prácticas 100%!</p>
            </div>
            <div class="service-card">
                <div class="service-icon">🌍</div>
                <h3>Inglés Básico</h3>
                <p>Conversación fluida desde el primer día. Método inmersivo para niños y adultos.</p>
            </div>
            <div class="service-card">
                <div class="service-icon">📚</div>
                <h3>Apoyo Escolar</h3>
                <p>Todos los niveles: primaria, secundaria y bachillerato. Refuerzo personalizado.</p>
            </div>
            <div class="service-card">
                <div class="service-icon">🎨</div>
                <h3>Trabajos Escolares</h3>
                <p>Maquetas, informes, diapositivas profesionales. ¡Entrega garantizada!</p>
            </div>
        </section>

        <!-- Plans -->
        <section class="plans">
            <h2 style="font-size: 2.5rem; margin-bottom: 20px; background: linear-gradient(45deg, var(--pastel-blue), var(--pastel-purple)); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">
                🕒 Nuestros Planes Flexibles
            </h2>
            <div class="plan-buttons">
                <button class="plan-btn plan-ausubel" onclick="showPlan('ausubel')">
                    📖 Plan Ausubel (4 días/semana)
                </button>
                <button class="plan-btn plan-piaget" onclick="showPlan('piaget')">
                    🎯 Plan Piaget (5 días/semana)
                </button>
            </div>
            <div class="plan-details" id="planDetail">
                <strong>2 horas diarias de aprendizaje intensivo</strong><br>
                <span style="color: #666;">Selecciona un plan para ver detalles</span>
            </div>
            <div class="limited-spots">⚠️ ¡CUPOS LIMITADOS! Solo quedan 3 lugares disponibles</div>
        </section>

        <!-- Testimonials -->
        <section class="testimonials">
            <h2 style="text-align: center; font-size: 2.5rem; margin-bottom: 40px;">
                ⭐ Lo que dicen nuestros padres
            </h2>
            <div class="testimonial">
                <div class="testimonial-avatar">M</div>
                <div class="testimonial-text">
                    "Mi hijo pasó de reprobar matemáticas a sacar 18/20. ¡Genio Visión es mágico!"<br>
                    <strong>- María F., Madre de Sebastián</strong>
                </div>
            </div>
            <div class="testimonial">
                <div class="testimonial-avatar">C</div>
                <div class="testimonial-text">
                    "Las clases de inglés son increíbles. Mi hija ya conversa sin miedo. ¡Recomendado 100%!"<br>
                    <strong>- Carlos R., Padre de Sofía</strong>
                </div>
            </div>
            <div class="testimonial">
                <div class="testimonial-avatar">A</div>
                <div class="testimonial-text">
                    "La mejor inversión que hice. Trabajo escolar perfecto y mi hijo ama aprender."<br>
                    <strong>- Ana L., Madre de Daniel</strong>
                </div>
            </div>
            <div class="testimonial">
                <div class="testimonial-avatar">J</div>
                <div class="testimonial-text">
                    "Profesionales excelentes. Mi hijo mejoró todas sus notas en solo 2 meses."<br>
                    <strong>- José M., Padre de Valentina</strong>
                </div>
            </div>
            <div class="testimonial">
                <div class="testimonial-avatar">L</div>
                <div class="testimonial-text">
                    "Computación básica explicada súper claro. ¡Ahora mi hijo hace todo solo!"<br>
                    <strong>- Laura G., Madre de Mateo</strong>
                </div>
            </div>
        </section>

        <!-- Contact Form -->
        <section class="contact-section">
            <h2 style="text-align: center; font-size: 2.5rem; margin-bottom: 40px;">
                💬 Déjanos tu opinión
            </h2>
            <form id="contactForm">
                <div class="form-group">
                    <label>Nombre completo *</label>
                    <input type="text" id="user_name" name="user_name" required>
                </div>
                <div class="form-group">
                    <label>Email *</label>
                    <input type="email" id="user_email" name="user_email" required>
                </div>
                <div class="form-group">
                    <label>Opinión / Sugerencia *</label>
                    <textarea id="message" name="message" rows="5" required 
                              placeholder="Cuéntanos tu experiencia con Genio Visión..."></textarea>
                </div>
                <button type="submit" class="btn-submit">📧 Enviar Opinión</button>
            </form>
            <p style="text-align: center; margin-top: 20px; font-size: 0.9rem; color: #666;">
                Tus datos llegarán directamente a <strong>jhoncuenta2@gmail.com</strong>
            </p>
        </section>

        <!-- Location -->
        <section class="location">
            <h2 style="font-size: 2.2rem; margin-bottom: 20px;">
                📍 Estado Carabobo, Los Guayos
            </h2>
            <p style="font-size: 1.2rem; margin-bottom: 20px;">
                Sector Félix Ríos, por el elevado
            </p>
            <div class="map-container">
                <iframe 
                    src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3928.123456789!2d-67.123456789!3d10.123456789!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zMTAwMDEmIzM5O3N0cmVldHM%3D!5e0!3m2!1ses!2sve!4v1735678901234!5m2!1ses!2sve" 
                    allowfullscreen="" loading="lazy">
                </iframe>
            </div>
        </section>

        <footer class="footer">
            <p style="font-size: 1.1rem; font-weight: 600;">
                📲 WhatsApp: <a href="https://wa.me/584244517644" style="color: #25D366; text-decoration: none;">+58 424-451-7644</a>
            </p>
            <p>🧠 Genio Visión - Donde los sueños educativos se hacen realidad</p>
        </footer>
    </div>

    <script>
        // EmailJS Configuration (Necesitas registrarte en emailjs.com gratis)
        emailjs.init("YOUR_PUBLIC_KEY"); // Reemplaza con tu clave pública de EmailJS

        document.getElementById('contactForm').addEventListener('submit', function(event) {
            event.preventDefault();
            
            emailjs.sendForm('YOUR_SERVICE_ID', 'YOUR_TEMPLATE_ID', this)
                .then(function() {
                    alert('¡Gracias por tu opinión! Hemos recibido tu mensaje.');
                    document.getElementById('contactForm').reset();
                }, function(error) {
                    alert('Hubo un error. Por favor intenta de nuevo o contáctanos por WhatsApp.');
                    console.log('FAILED...', error);
                });
        });

        function showPlan(plan) {
            const detail = document.getElementById('planDetail');
            if (plan === 'ausubel') {
                detail.innerHTML = '<strong>Plan Ausubel - 4 días/semana</strong><br>16 horas semanales de aprendizaje intensivo<br><span style="color: #4CAF50;">💰 Precio especial disponible por WhatsApp</span>';
            } else {
                detail.innerHTML = '<strong>Plan Piaget - 5 días/semana</strong><br>20 horas semanales de máximo rendimiento<br><span style="color: #9C27B0;">💎 Plan Premium con seguimiento personalizado</span>';
            }
        }

        // Smooth scroll y efectos
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>
